<?xml version = "1.0" encoding="UTF-8" ?>
<archive>
    <pages>
    {% for page in pages %}
        <page>
            <title>{{ page.title|escape }}</title>
            <url>/page/{{ page.slug|escape }}/</url>
            <date>{{ page.list_date.date }}</date>
        </page>
    {% endfor %}
    </pages>
</archive>
